---
const {label, placeholder, name} = Astro.props;
---

<label for={name} class="flex gap-2 flex-col">{label}
    <input type="text" id={name} placeholder={placeholder} name={name} required
        class="bg-zinc-900 px-3 py-3 rounded-lg focus:outline-blue-500 focus:outline w-lg h-12"/>
</label>